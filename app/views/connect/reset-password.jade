.flex.center.middle.connect-register-container.tc-ui("role"="main")
  login-reg-shell
    header.flex.center.middle
      img(src=require("../../images/logo_mobile.svg"))

    main.light-bg.elevated-bottom
      reset-password
        div(ng-show="!vm.success")
          h1 reset password

          p.secondary Please enter your new password.

          p.error(ng-show="vm.error") {{ vm.error }}

          form(ng-submit="vm.submit()")
            input.input.input-sm(type="password" ng-model="vm.password" required=true placeholder="New Password"  autofocus=true)

            button.action.tc-btn.tc-btn-primary(type="submit") Reset password

        p.success(ng-show="vm.success") Your password has been updated.
        p <a ui-sref="CONNECT_LOGIN">Back to Login</a>

    footer
      p.copyright © 2017 topcoder. All Rights Reserved


